describe('{{pascalCase pageName}} page', () => {
  beforeEach(() => {
    cy.visit('http://localhost:3000/{{kebabCase pageName}}');
  });

  it('should visit to the {{pascalCase pageName}} page', () => {
    cy.contains('Coffe Delivery').should('be.visible');
  });
});
